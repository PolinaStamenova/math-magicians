/* eslint-disable react/prefer-stateless-function, quotes */
import React from "react";
import "./Quote.css";

export default function Quote() {
  return (
    <p className="quote">
      Now save your file and test your app by navigating from the index page to
      the about page and then to a non-existing page. It should work as
      intended.
    </p>
  );
}
